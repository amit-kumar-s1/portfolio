# Used by server components during rendering. Can point at an internal address.
API_URL=http://127.0.0.1:8000
# Used by the contact form in the browser. Must be publicly reachable over HTTPS in production.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
